ALTER TABLE `users` ADD `qualified` INT NULL DEFAULT NULL COMMENT '1 = Yes, I am qualified in at least one of the above,2=I am still training to qualify in one of the above,3=No I am not, but interested to train in one of those' AFTER `region_id`, ADD `category_id` VARCHAR(255) NULL DEFAULT NULL AFTER `qualified`, ADD `treatment_id` VARCHAR(255) NULL DEFAULT NULL AFTER `category_id`, ADD `gender` INT NULL DEFAULT NULL COMMENT '0= Male, 1 = Female' AFTER `treatment_id`, ADD `year` VARCHAR(255) NULL DEFAULT NULL AFTER `gender`, ADD `provide_treatment` INT NULL DEFAULT NULL COMMENT '1 = Yes, 0 = No' AFTER `year`, ADD `transport_mode` INT NULL DEFAULT NULL COMMENT '1 = Public Transport ,2 = Driving' AFTER `provide_treatment`;
